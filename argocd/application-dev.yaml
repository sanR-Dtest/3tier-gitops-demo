apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ecommerce-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/sanR-Dtest/3tier-gitops-demo
    targetRevision: main
    path: charts/ecommerce
    helm:
      releaseName: ecommerce-dev
      values: |
        environment: dev
  destination:
    server: https://kubernetes.default.svc
    namespace: ecommerce
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
